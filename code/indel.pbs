#!/bin/bash

#PBS -l walltime=11:00:00
#PBS -j oe


module load samtools/1.10

samtools view -@ ${THREADS} ${BAM} > ${OD}/${NAME}.sam
python3 ${WD}/code/indel_bias.py ${OD}/${NAME}.sam
rm ${OD}/${NAME}.sam

